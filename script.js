function abrirMenu() {
        // Abre o menu, definindo a largura para 250px
        document.getElementById("menuLateral").style.width = "250px";
    }

    function fecharMenu() {
        // Fecha o menu, definindo a largura para 0
        document.getElementById("menuLateral").style.width = "0";
    }
